import React from 'react';
import '@testing-library/jest-dom';

// This test file ensures there's at least one test to run
// Prevents the "Your test suite must contain at least one test" error for type declarations

describe('Test Environment Setup', () => {
  it('should be properly configured', () => {
    expect(true).toBe(true);
  });
}); 